import * as React from "react";
import { LayerSide } from "./types";
export declare type ArrowProps = {
    angle?: number;
    size?: number;
    roundness?: number;
    borderWidth?: number;
    borderColor?: string;
    backgroundColor?: string;
    layerSide?: LayerSide;
    style?: React.CSSProperties;
};
export declare const Arrow: ({ size, angle, borderWidth, borderColor, roundness, backgroundColor, layerSide, style }: ArrowProps) => JSX.Element | null;
export default Arrow;
